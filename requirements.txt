# SHIFT — synthetic pre-pilot district-heating flexibility simulator
# Install with:  pip install -r requirements.txt
#
# The simulation engine and HTTP server use the Python standard library only.
# numpy / pandas / any web framework are deliberately NOT dependencies.

pyyaml>=6.0        # configuration loading
pytest>=7.0        # test suite
